name: Daily Instagram Post

on:
  schedule:
    # 18:55 UTC = 20:55 CEST in summer, 19:55 CET in winter.
    # Instagram only needs the image public before publish, so the 20:00 target
    # is close enough. Adjust as desired.
    - cron: "55 18 * * *"
  workflow_dispatch:
    inputs:
      post_type:
        description: "Override post type (leave blank for today's rotation)"
        required: false
        type: choice
        default: ""
        options:
          - ""
          - market_recap
          - gainers_losers
          - commodities_forex
          - weekly_recap
          - news_digest
      dry_run:
        description: "Render only, don't publish"
        required: false
        type: boolean
        default: false

permissions:
  contents: write  # needed to commit the generated PNG so raw.githubusercontent serves it

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Render image
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          BRAND_NAME: ${{ vars.BRAND_NAME }}
          BRAND_HANDLE: ${{ vars.BRAND_HANDLE }}
          DRY_RUN: "1"  # render-only pass
        run: |
          args=""
          if [ -n "${{ inputs.post_type }}" ]; then
            args="--type ${{ inputs.post_type }}"
          fi
          python src/main.py --dry-run $args

      - name: Commit generated image
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add output/*.png || true
          if ! git diff --cached --quiet; then
            git commit -m "chore(post): daily image $(date -u +%Y-%m-%d)"
            for i in 1 2 3; do
              git pull --rebase --autostash origin "${GITHUB_REF_NAME:-main}" && git push && break || true
              echo "Push attempt $i failed, retrying after sleep..."
              sleep 5
            done
          else
            echo "No new image to commit."
          fi

      - name: Publish to Instagram
        if: ${{ !inputs.dry_run }}
        env:
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
          IG_BUSINESS_ACCOUNT_ID: ${{ secrets.IG_BUSINESS_ACCOUNT_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GITHUB_REPO: ${{ github.repository }}
          GITHUB_REF_NAME: ${{ github.ref_name }}
          BRAND_NAME: ${{ vars.BRAND_NAME }}
          BRAND_HANDLE: ${{ vars.BRAND_HANDLE }}
        run: |
          args=""
          if [ -n "${{ inputs.post_type }}" ]; then
            args="--type ${{ inputs.post_type }}"
          fi
          python src/main.py $args
